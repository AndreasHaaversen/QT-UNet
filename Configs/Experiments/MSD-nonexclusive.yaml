experiment: "MSD"

model: !include Models/QT-UNet/Tiny.yaml
model_extra:
  exclusive_targets: False

data:
  batch_size: 1
  num_workers: 6
  task: "Task01"

trainer:
  max_epochs: 350

metrics:
  list:
    - "Dice"
  include_background: False
